<can-component tag="pmo-restaurant-details">
  <style type="less">
    display: block;
    
    p { font-weight: bold; }
  </style>
  <template>
    <p>{{message}}</p>
  </template>
  <view-model>
    import Map from 'can/map/';
    import 'can/map/define/';

    export default Map.extend({
      define: {
        message: {
          value: 'This is the pmo-restaurant-details component'
        }
      }
    });
  </view-model>
</can-component>